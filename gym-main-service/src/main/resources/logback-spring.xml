<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 1. Console Appender Configuration -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [TxId: %X{transactionId}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 2. Rolling File Appender Configuration -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/gym-crm.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/gym-crm.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [TxId: %X{transactionId}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 3. Prevent Spring framework from spamming logs in any profile -->
    <logger name="org.springframework" level="WARN"/>

    <!-- SPRING ENVIRONMENT PROFILES                -->

    <!-- PROFILE: local or dev (for development) -->
    <springProfile name="local, dev">
        <!-- Detailed logs for application code -->
        <logger name="com.epam.gym" level="DEBUG"/>

        <!-- Default log level for everything else -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <!-- PROFILE: prod (for production) -->
    <springProfile name="prod">
        <!-- No DEBUG logs in production, keep only important messages -->
        <logger name="com.epam.gym" level="INFO"/>

        <!-- Set default level for other libraries to WARN to save disk space -->
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

</configuration>